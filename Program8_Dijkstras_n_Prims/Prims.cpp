#include <stdio.h>

int main(int argc, char *argv[])
{
	printf("Hello, world\n");



  MSTPrims(int x)
  {
      /*Initialize all distances to be very far; and everyone's an orphan*/
    while (vertex v)
    {
      v.distance = MAX_INT; // book calls this 'key'
      v.parent   = NULL;      
    }
    vertices[x].distance = 0; // root has zero distance from itself

    min-buildheap;
    min-heapsort;
    min-queue;
  
    vertex U;
    while (Q.next != NULL)
    {
      U = Q.extract;
      while (u.next != NULL)
      {
        if (that edge.length < edge.distance) ???? whats really going on here? ?????
        {
        }

      }

    }



  }











	
	return 0;
}
